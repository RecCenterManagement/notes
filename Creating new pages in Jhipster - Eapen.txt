1) Add an ErrorBoundaryRoute in route.tsx
2) Create a new folder for the module page in the module directory along with the file necessary (.tsx, .scss)
3) add an export default at the end of the files
3) add an import of the new module in route.tsx

It should be in JHipster now
